# copilot-instructions

- `~/.copilot` を直接参照しないかわりに、`nix/home/base/cli/copilot-cli` を参照すること
  - `~/.copilot` 内のファイルはすべて `nix/home/base/cli/copilot-cli` からエイリアスを張っているため
- バックログファイルは./.ichi-hディレクトリにて管理されている
