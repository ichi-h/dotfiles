if [ $# -ne 1 ]; then
  echo "Usage: wol <win or arch>" 1>&2
  exit 1
fi

if [ $1 = win ]; then
  wakeonlan $WIN_MAC_ADDRESS
elif [ $1 = arch ]; then
  wakeonlan $ARCH_MAC_ADDRESS
else
  echo "Invalid argument, must be 'win' or 'arch'." 1>&2
fi
