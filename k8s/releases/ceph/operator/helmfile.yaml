environments:
  dev:
    values:
      - enabled: true
  prod:
    values:
      - enabled: false
---
releases:
  - name: rook-ceph
    chart: rook-release/rook-ceph
    version: 1.18.2
    condition: enabled
    namespace: rook-ceph
    createNamespace: true
    values:
      - ./values.yaml
