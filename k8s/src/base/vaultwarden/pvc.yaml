kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: vaultwarden-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: vaultwarden-local-storage
  resources:
    requests:
      storage: 128Mi
